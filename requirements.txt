# CUSTOS — Python dependencies
# Install: pip install -r requirements.txt

# ── Core AI & Vision ──────────────────────────────────────
ultralytics>=8.0.0       # YOLOv8
opencv-python>=4.8.0     # Camera capture + frame processing
numpy>=1.24.0            # Array ops (used by zone_monitor)
lapx                     # Required by Ultralytics for ByteTrack
scipy

# ── Web Server & Auth ─────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0  # Required by flask-socketio
Flask-SQLAlchemy>=3.1.0  # Database ORM
Flask-Login>=0.6.3       # Session management
bcrypt>=4.0.0            # Password hashing

# ── Alerts ────────────────────────────────────────────────
python-telegram-bot>=20.0
python-dotenv>=1.0.0     # Loads .env file for credentials

# ── Optional: Desktop notifications + alarm sound ─────────
# Uncomment if you want local popups and beep on alert
# plyer>=2.1.0
# pygame>=2.5.0

# ── Authentication & Updater ──────────────────────────────
Authlib>=1.3.0
requests>=2.31.0

# ── Production Server & Testing ───────────────────────────
waitress>=2.1.0
eventlet>=0.33.0
pytest>=7.0.0